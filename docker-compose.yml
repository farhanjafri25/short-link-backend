version: '3.8'
services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: users
  short-link-app:
    build:
      context: .
      args:
        NODE_VERSION: "16"
    ports:
      - "3002:3002"
    depends_on:
      - postgres
    environment:
      - POSTGRES_HOST=localhost
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=users